import {createSlice} from '@reduxjs/toolkit'

const initialState ={
    value:{}
}

export const caritasSlice = createSlice({
    name:'pruebaCaritas',
    initialState,
    reducers:{
        agregarFila:(state)=>{value}
    },
});